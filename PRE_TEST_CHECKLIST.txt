╔════════════════════════════════════════════════════════════════════════════════╗
║                                                                                ║
║               PRE-TEST CHECKLIST - Before Testing Payment                     ║
║                                                                                ║
║                    Make sure everything is configured correctly              ║
║                                                                                ║
╚════════════════════════════════════════════════════════════════════════════════╝


📋 BACKEND SETUP
═══════════════════════════════════════════════════════════════════════════════

Essential Files to Check:

☐ server/.env exists and contains:
  
  RAZORPAY_API_KEY=rzp_test_RwhO9fIZN01Muu
  RAZORPAY_API_SECRET=32b8X1Mx4VLUfyjKS2LL3gaP
  
  ⚠️ IMPORTANT: Check these values match your Razorpay dashboard!
  ⚠️ DO NOT use test keys shown above - use YOUR OWN from Razorpay

☐ server/config/razorpay.js exists with:
  - import Razorpay from 'razorpay'
  - Initialized with API_KEY and API_SECRET
  - Exported as default

☐ server/controllers/order.controller.js has:
  - paymentController: Creates Razorpay order
  - verifyPaymentController: Verifies signature, creates order, clears cart
  - webhookRazorpay: Optional webhook handler

☐ server/route/order.route.js has:
  - POST /checkout with auth middleware → paymentController
  - POST /verify-payment with auth middleware → verifyPaymentController
  - POST /razorpay-webhook → webhookRazorpay

☐ Auth middleware is attached to payment routes:
  - orderRouter.post('/checkout', auth, paymentController)
  - orderRouter.post('/verify-payment', auth, verifyPaymentController)


═══════════════════════════════════════════════════════════════════════════════

📋 FRONTEND SETUP
═══════════════════════════════════════════════════════════════════════════════

Essential Files to Check:

☐ client/src/pages/CheckoutPage.jsx has:
  - loadRazorpayScript() function
  - handleRazorpayPayment() function
  - Button that calls handleRazorpayPayment onClick

☐ CheckoutPage has all these imports:
  - import { useNavigate } from 'react-router-dom'
  - import { toast } from 'react-hot-toast'
  - Axios import
  - SummaryApi import

☐ CheckoutPage has these useState hooks:
  - const [isLoading, setIsLoading] = useState(false)
  - const [selectAddress, setSelectAddress] = useState(0)
  - const [cartItemsList, setCartItemsList] = useState([])
  - const [totalPrice, setTotalPrice] = useState(0)
  - And any others from your code

☐ client/src/common/SummaryApi.js has:
  - payment_url endpoint (for checkout)
  - verifyPayment endpoint (for verification)

☐ Routes exist:
  - /success route (should have Success.jsx or similar)
  - /cancel route (should have Cancel.jsx or similar)


═══════════════════════════════════════════════════════════════════════════════

📋 ENVIRONMENT & DATABASE
═══════════════════════════════════════════════════════════════════════════════

☐ MongoDB is running:
  - Should see connection message in backend console
  - No database connection errors

☐ Backend is running:
  - npm run dev in server/ folder
  - Should see "Server running on port 8080" (or your port)
  - No startup errors

☐ Frontend is running:
  - npm run dev in client/ folder
  - Should open on http://localhost:5173 (or your port)
  - No console errors on startup

☐ You are logged in:
  - Login to your account
  - Check browser console: No "Unauthorized" errors
  - JWT token in localStorage or cookies

☐ Cart has items:
  - Add some products to cart
  - Cart shows correct items and prices
  - Can proceed to checkout


═══════════════════════════════════════════════════════════════════════════════

📋 RAZORPAY ACCOUNT SETUP
═══════════════════════════════════════════════════════════════════════════════

☐ Razorpay Account Setup:
  - You have a Razorpay business account
  - Test mode is enabled
  - You can see test keys in dashboard

☐ Test Keys Copied:
  - Copy Key ID from Razorpay dashboard
  - Paste in server/.env as RAZORPAY_API_KEY
  - Copy Key Secret from Razorpay dashboard
  - Paste in server/.env as RAZORPAY_API_SECRET

☐ Test Credentials Ready:
  Test Card: 4111 1111 1111 1111
  Expiry: Any future date (e.g., 12/25)
  CVV: Any 3 digits (e.g., 123)
  OTP: 123456 (usually accepted in test mode)


═══════════════════════════════════════════════════════════════════════════════

📋 BROWSER & NETWORK
═══════════════════════════════════════════════════════════════════════════════

☐ Internet Connection:
  - Strong connection
  - Can reach Razorpay CDN (https://checkout.razorpay.com)

☐ Browser Console Clean:
  - Open DevTools: F12
  - Console tab: No red errors on startup
  - Only warnings are okay

☐ Network Tab Ready:
  - DevTools open
  - Network tab visible
  - XHR/Fetch filter active

☐ Cookies Cleared:
  - Clear all cookies for localhost
  - Clear all localStorage
  - Restart browser

☐ Not Using VPN/Proxy:
  - Some payment APIs block VPNs
  - Use direct connection for testing


═══════════════════════════════════════════════════════════════════════════════

📋 MODELS & DATABASE
═══════════════════════════════════════════════════════════════════════════════

☐ Order Model exists with fields:
  - userId
  - orderId
  - productId
  - product_details
  - paymentId
  - payment_status
  - delivery_address
  - subTotalAmt
  - totalAmt

☐ Cart Model exists with fields:
  - userId
  - productId (with populate)
  - quantity
  - price (or calculated from productId)

☐ User Model has:
  - _id field
  - email field
  - shopping_cart array
  - Can be updated to empty array

☐ Address Model exists with fields:
  - _id
  - userId
  - address_line1
  - address_line2
  - city
  - state
  - country
  - pincode
  - mobile
  - status (true = active)


═══════════════════════════════════════════════════════════════════════════════

✅ QUICK VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════════════════════════

To quickly verify your setup, run these in terminal:

1. Check backend running:
   curl http://localhost:8080/api/order/order-list
   
   Should return error (because no auth) but not "connection refused"

2. Check frontend running:
   curl http://localhost:5173
   
   Should return HTML content

3. Check .env file:
   cat server/.env | grep RAZORPAY
   
   Should show your keys (don't share these!)

4. Check backend logs:
   Look at terminal where you ran "npm run dev"
   Should see: "Server running on port 8080"


═══════════════════════════════════════════════════════════════════════════════

🚀 READY TO TEST?
═══════════════════════════════════════════════════════════════════════════════

Once you've checked all items above:

1. Make sure backend is running: npm run dev (in server/)
2. Make sure frontend is running: npm run dev (in client/)
3. Open browser to http://localhost:5173
4. Login to your account
5. Add items to cart
6. Go to Checkout
7. Select address
8. Click "Online Payment"
9. Open Console: F12
10. Watch logs as payment processes
11. Use test card: 4111 1111 1111 1111
12. Enter any date and CVV
13. Enter OTP: 123456
14. Check if:
    ✅ Console shows "Navigating to success page..."
    ✅ Redirects to /success
    ✅ Button no longer shows "Processing..."
    ✅ Cart is empty
    ✅ Order appears in MyOrders


═══════════════════════════════════════════════════════════════════════════════

📞 IF YOU NEED HELP
═══════════════════════════════════════════════════════════════════════════════

When contacting support, provide:

1. Screenshot of console logs (where it stops)
2. Backend console output during payment
3. Network tab showing /verify-payment response
4. server/.env (redact actual keys)
5. Error messages shown
6. Steps you followed

This will help identify the exact issue quickly.

═══════════════════════════════════════════════════════════════════════════════
