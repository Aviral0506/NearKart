apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nearkart-ingress
  annotations:
    # NGINX Ingress Controller is default in Minikube
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 5000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
